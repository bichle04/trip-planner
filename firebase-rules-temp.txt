// Temporary Firebase Rules for testing (KHÔNG dùng trong production)
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Cho phép đọc tất cả collections để test
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
